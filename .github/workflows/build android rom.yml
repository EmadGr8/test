name: "Build Android ROM"
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build:
    name: "Build Android ROM"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up JDK
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'
      - name: Install required packages
        run: |
          sudo apt update
          sudo apt upgrade
          sudo apt-get install git-core
          git clone https://github.com/akhilnarang/scripts
          cd scripts
          mkdir -p ~/bin
          mkdir -p ~/android/lineage
          curl https://storage.googleapis.com/git-repo-downloads/repo > ~/bin/repo
          chmod a+x ~/bin/repo
          cd ~/android/lineage
      - name: Clone and sync ROM repository
        run: |
          git clone https://github.com/LineageOS/android.git
          cd LineageOS
          repo init -u https://github.com/LineageOS/android.git -b lineage-20.0
          repo sync --current-branch --no-tags --no-clone-bundle --optimized-fetch --force-broken
          git clone https://github.com/EmadGr8/device_xiaomi_twolip.git -b LOS device/xiaomi/twolip
          git clone https://github.com/EmadGr8/vendor_xiaomi_twolip.git -b tiramisu vendor/xiaomi/twolip
          git clone https://github.com/EmadGr8/kernel_xiaomi_sdm660.git -b 13 kernel/xiaomi/sdm660
      - name: Build ROM
        run: |
          source build/envsetup.sh
          lunch lineage_twolip-user
          make -j$(nproc) bacon
      - name: Upload ROM artifact
        uses: actions/upload-artifact@v2
        with:
          name: ROM
          path: lineage/out/target/product/target_device/*.zip
